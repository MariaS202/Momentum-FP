import { View, Text, StyleSheet} from "react-native";
export default function Labels() {
    return (
        <View style={styles.container}>
            <Text>WELCOME LLAAAABEEEEELLLLLs</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: 'white',
        padding: 20,
    },

});